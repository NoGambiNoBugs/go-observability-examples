// Code generated by gowrap. DO NOT EDIT.
// template: ../../templates/red_template.go.tmpl
// gowrap: http://github.com/hexdigest/gowrap

package red

import (
	"context"
	"fmt"
	"os"
	"strings"
	"time"

	"github.com/NoGambiNoBugs/go-observability-examples/internal/entity"
	"github.com/NoGambiNoBugs/go-observability-examples/internal/instrumentation"
	"github.com/NoGambiNoBugs/go-observability-examples/internal/port"
	"github.com/prometheus/client_golang/prometheus"
)

type CustomerUsecaseWithRED struct {
	base         port.CustomerUsecase
	histogramVec *prometheus.HistogramVec
}

// Create implements port.CustomerUsecase
func (d CustomerUsecaseWithRED) Create(ctx context.Context, customer entity.Customer) (err error) {
	since := time.Now()
	defer func() {
		status := "ok"
		if err != nil {
			status = "error"
		}

		labels := prometheus.Labels{
			"status": status,
			"method": "Create",
		}

		observer, err := d.histogramVec.GetMetricWith(labels)
		if err != nil {
			fmt.Printf("Metric: Error to get metric with labels %v\n", labels)
		}

		observer.Observe(float64(time.Since(since).Milliseconds()))
	}()
	return d.base.Create(ctx, customer)
}

// NewCustomerUsecaseWithRED returns an instance of the port.CustomerUsecase decorated with red histogram metric
func NewCustomerUsecaseWithRED(base port.CustomerUsecase, subsystem string, constLabels prometheus.Labels) (decorator port.CustomerUsecase, err error) {
	decorate := os.Getenv("DECORATE")
	if !(decorate == "true" || decorate == "1") {
		return base, nil
	}

	metricConfig := prometheus.HistogramOpts{
		Namespace:   strings.TrimSpace("example"),
		Subsystem:   strings.TrimSpace(subsystem),
		Name:        strings.TrimSpace("customer_usecase_red"),
		Help:        "CustomerUsecase RED histogram (rate, errors and duration).",
		ConstLabels: constLabels,
		Buckets:     prometheus.ExponentialBuckets(100, 2, 5),
	}

	red := CustomerUsecaseWithRED{
		base:         base,
		histogramVec: prometheus.NewHistogramVec(metricConfig, []string{"status", "method"}),
	}

	err = instrumentation.Registry.Register(red.histogramVec)
	if err != nil {
		return nil, err
	}

	return red, nil
}
